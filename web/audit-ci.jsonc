{
  // Fail CI on high or critical npm advisories.
  "high": true

  // No allowlist: the fabric@5 advisory tree (fabric, canvas, tar,
  // @mapbox/node-pre-gyp) was cleared by the fabric 5 -> 7 upgrade.
}
